<Project Sdk="Microsoft.NET.Sdk">
  <!--
    Caricatore "a caldo" di sayRevit. È l'unico assembly che Revit carica direttamente (e quindi
    blocca su disco): a ogni clic sul pulsante legge SayRevit.Addin.dll e le sue dipendenze dalla
    cartella di installazione, le carica in memoria ed esegue il comando. Così l'add-in si può
    ricompilare e reinstallare con Revit aperto: il codice nuovo gira dal clic successivo.
    Stesse regole di compilazione dell'add-in (RevitVersion / RevitFramework / RevitApiDir).
  -->
  <PropertyGroup>
    <RevitVersion Condition="'$(RevitVersion)' == ''">2025</RevitVersion>
    <RevitFramework Condition="'$(RevitFramework)' == '' And '$(RevitVersion)' == '2024'">net48</RevitFramework>
    <RevitFramework Condition="'$(RevitFramework)' == '' And ('$(RevitVersion)' == '2025' Or '$(RevitVersion)' == '2026')">net8.0-windows</RevitFramework>
    <RevitFramework Condition="'$(RevitFramework)' == ''">net10.0-windows</RevitFramework>
    <TargetFramework>$(RevitFramework)</TargetFramework>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <RootNamespace>SayRevit.Loader</RootNamespace>
    <AssemblyName>SayRevit.Loader</AssemblyName>
    <PlatformTarget>x64</PlatformTarget>
    <OutputPath>../../artifacts/$(RevitVersion)/</OutputPath>
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <GenerateDependencyFile>false</GenerateDependencyFile>
    <Description>sayRevit: caricatore a caldo dell'add-in.</Description>
  </PropertyGroup>
  <ItemGroup Condition="'$(RevitApiDir)' != ''">
    <Reference Include="RevitAPI">
      <HintPath>$(RevitApiDir)/RevitAPI.dll</HintPath>
      <Private>false</Private>
    </Reference>
    <Reference Include="RevitAPIUI">
      <HintPath>$(RevitApiDir)/RevitAPIUI.dll</HintPath>
      <Private>false</Private>
    </Reference>
  </ItemGroup>
  <ItemGroup Condition="'$(RevitApiDir)' == ''">
    <PackageReference Include="Nice3point.Revit.Api.RevitAPI" Version="$(RevitVersion).*" />
    <PackageReference Include="Nice3point.Revit.Api.RevitAPIUI" Version="$(RevitVersion).*" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' == 'net48'">
    <Reference Include="PresentationCore" />
    <Reference Include="PresentationFramework" />
    <Reference Include="WindowsBase" />
    <Reference Include="System.Xaml" />
  </ItemGroup>
  <ItemGroup Condition="'$(TargetFramework)' != 'net48'">
    <FrameworkReference Include="Microsoft.WindowsDesktop.App.WPF" />
  </ItemGroup>
  <ItemGroup>
    <None Include="SayRevit.addin" CopyToOutputDirectory="PreserveNewest" />
  </ItemGroup>
</Project>
